import ReferralModel, { IReferral } from "./referral.model";
import RewardModel, { IReward } from "./reward.model";
import WalletModel, { IWallet } from "./wallet.model";

export {
    ReferralModel,
    RewardModel,
    WalletModel,

    IReferral,
    IReward,
    IWallet
}